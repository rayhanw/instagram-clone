function changeContent(innerHTML) {
  const likeWrapper = document.getElementById('like-cta-<%= @like.post.id %>');

  likeWrapper.innerHTML = innerHTML
}

changeContent("<%= j render 'create', post: @like.post %>")
