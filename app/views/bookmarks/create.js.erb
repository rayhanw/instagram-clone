function changeContent(innerHTML) {
  const bookmarkWrapper = document.getElementById('bookmark-cta-<%= @post.id %>');

  bookmarkWrapper.innerHTML = innerHTML
}

changeContent("<%= j render 'destroy', bookmark: @bookmark %>")
