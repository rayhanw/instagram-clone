<div class="flex items-center mt-4">
  <%= render partial: "shared/avatar", locals: { avatar_class: "avatar", user: other_person } %>
  <div class="flex flex-col ml-4">
    <h4 class="font-bold"><%= other_person.username %></h4>
    <span><%= direct_message.latest_message.content %> • <%= time_ago_in_words(direct_message.latest_message.created_at) %></span>
  </div>
</div>
