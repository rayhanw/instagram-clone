<%= render partial: 'shared/top_nav' %>

<div class="mx-3 mt-4">
  <div id="comments" data-post-id="<%= @post.id %>">
    <% @presenter.comments.each do |comment| %>
      <%= render partial: 'content', locals: { comment: comment, first: @presenter.comments[0] == comment } %>
    <% end %>
  </div>
  <%= render partial: 'form', locals: { post: @presenter.post, comment: @presenter.new_comment } %>
</div>
